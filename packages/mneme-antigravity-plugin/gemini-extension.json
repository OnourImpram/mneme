{
  "name": "mneme",
  "version": "3.0.1",
  "description": "Vault-native memory for Antigravity. Markdown is ground truth. Hybrid retrieval, temporal knowledge graph, zero LLM cost on Stop.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "mneme": {
      "command": "mneme-mcp",
      "env": {
        "MNEME_VAULT": "${MNEME_VAULT}"
      }
    }
  }
}
